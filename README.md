# React + TypeScript + Vite

## Heroes App

### Run application in dev

1. Clone the repository
2. Create a `.env` file based on `.env.example` and edit the variables
3. Execute `npm install`
4. Execute `npm run dev`